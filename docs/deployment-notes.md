# Deployment Notes

Base Sepolia operator wallet:

```text
0xD0C3228b18AEfCcD2D897fFA5a3a75f46b0975Fa
```

This address is public and safe to document. Do not commit or share the matching private key.

Local RainbowKit WalletConnect project ID is stored in `.env.local`, which is intentionally git-ignored.
